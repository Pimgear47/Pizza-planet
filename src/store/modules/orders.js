const state = {
    orders: [],
}

const getters = {

}

const mutations = {
    addOrder: (state, orders) => state.orders.push(orders)
}

const actions = {

}

export default {
    state,
    getters,
    mutations,
    actions
}